export default function BrandMark() {
  return (
    // Keep the brand mark isolated so the top-left identity can be moved as one object.
    // The text stays plain because the reference relies on spacing and weight more than ornament.
    <h1 className="brand-mark">COINCHIP</h1>
  )
}
